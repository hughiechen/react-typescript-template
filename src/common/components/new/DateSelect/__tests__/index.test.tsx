// import React from 'react'
// import { render } from '@testing-library/react'
import '@testing-library/jest-dom/extend-expect'

// TODO: 重新设计实现

// import DateSelect from '../index'
// import moment, { Moment } from 'moment'

describe('DateSelect', () => {
  // test('custom', () => {
  //   const value: [Moment, Moment] = [moment('2020-06-29').startOf('day'), moment('2020-06-29').endOf('day')]
  //   const onChange = jest.fn()
  //   const { asFragment } = render(<DateSelect defaultValue={{ type: 'custom', value }} onChange={onChange} />)
  //   expect(asFragment()).toMatchSnapshot()
  // })
  // test('today', () => {
  //   const value: [Moment, Moment] = [moment('2020-06-29').startOf('day'), moment('2020-06-30').endOf('day')]
  //   const onChange = jest.fn()
  //   const { asFragment } = render(<DateSelect defaultValue={{ type: 'today', value }} onChange={onChange} />)
  //   expect(asFragment()).toMatchSnapshot()
  // })
})
